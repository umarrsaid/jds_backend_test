// cron.js
// const PredictionJob = require(appDir + '/app/cron/Prediction/PredictionJob');
// const HostnameJob = require(appDir + '/app/cron/ServerGroup/HostnameJob');
// const ClearDataJob = require(appDir + '/app/cron/Prediction/ClearDataJob');

const cron = function() {
  // PredictionJob('5 * * * * *');
  // HostnameJob('1 * * * * *');
  // ClearDataJob('1 * * * * *');
};

module.exports = cron;
